numpy==2.2.6
pillow>=10.3
requests>=2.32
huggingface_hub>=0.24
tqdm>=4.66
packaging>=24.0

# --- S3 업로드 ---
boto3>=1.34

# --- Runpod serverless SDK ---
# runpod>=1.8.0

# --- 커스텀 CUDA 확장(예: custom_rasterizer) 빌드 보조 ---
pybind11>=2.12
ninja>=1.11
setuptools>=69.0
wheel>=0.43

# --- LTX-Video / 영상 관련 ---
einops>=0.7
imageio>=2.34
imageio-ffmpeg>=0.4
av>=12.0
# decord는 리눅스에서만 휠이 깔끔하게 제공, 필요 시 주석 해제
decord==0.6.0 ; platform_system != "Windows"

# --- (선택) 모델 로더/가중치 포맷 보조 ---
safetensors>=0.4
